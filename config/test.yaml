knex:
  useNullAsDefault: true
  client: 'sqlite3'
  connection:
    filename: ':memory:'
manifest:
  server:
    autoListen: false
    routes:
      cors:
        origin: 'ignore'
    router:
      stripTrailingSlash: true
jwtSecret: 'secret'
log:
  level: 'warn'
